@page
@using netOnTheWeb.Models;
@model netOnTheWeb.Pages.HelloWebModel
@{
}

<h1 style="margin-bottom: 30px; text-align: center"> Students </h1>


<p>@Model.Message The time is @DateTime.Now</p>


<table class="table table-striped table-wide">

    <thead style="border-bottom:solid red 2px">
    <th width="20%">ID</th>
    <th width="20%">NAME</th>
    <th width="20%">CLASS ID</th>
    </thead>
    <tbody>

        @foreach (Student student in @Model.studenstsList.getAllStudents())
        {
            <tr>
                <td>@student.getId()</td>
                <td>@student.getName()</td>
                <td>@student.getStudentClass().getClassId()</td>
            </tr>
        }

        @*<tr style="border-bottom: solid blue 1px">
            <td>@Model.studenstsList.getStudentFromList(0).getId()</td>
            <td>@Model.studenstsList.getStudentFromList(0).getName()</td>
            <td>@Model.studenstsList.getStudentFromList(0).getStudentClass().getClassId()</td>
        </tr>
        <tr style="border-bottom: solid blue 1px">
            <td>@Model.studenstsList.getStudentFromList(1).getId()</td>
            <td>@Model.studenstsList.getStudentFromList(1).getName()</td>
            <td>@Model.studenstsList.getStudentFromList(1).getStudentClass().getClassId()</td>
        </tr>
        <tr style="border-bottom: solid blue 1px">
            <td>@Model.studenstsList.getStudentFromList(2).getId()</td>
            <td>@Model.studenstsList.getStudentFromList(2).getName()</td>
            <td>@Model.studenstsList.getStudentFromList(2).getStudentClass().getClassId()</td>
        </tr>*@

    </tbody>
</table>